import time
import winsound

def focus():
    while True:
        time.sleep(25*60)
        winsound.Beep(1000, 1000)
        if input("是否开始休息？（输入y或n）") == "y":
            time.sleep(5*60)
        else:
            break

if __name__ == '__main__':
    focus()
